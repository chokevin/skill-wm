# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets
.env

# Collected rollouts and trained checkpoints
data/rollouts/
data/checkpoints/
data/eval/

# rune-py local mode lands outputs under cwd/datasets and cwd/checkpoints
# (matching ctx.durable_datasets_dir / ctx.durable_checkpoints_dir defaults).
datasets/
checkpoints/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Eval outputs and LLM cache
eval-*.json
.skill_wm_llm_cache.json

